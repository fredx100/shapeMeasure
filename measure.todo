Brief {
   > Measure 2D shapes using depth measuring.
   > Output shape in CAD-compatible format
}

Choices {
   ? Output format
      > svg
      > dxf
   ? Language
      > Javascript
         > Easy svg and display
         > Portable
      > Python
         > Easy svg output but no easy display
         > semi-portable
      > C++
         > familiar
         > Easy svg output
         > Not portable
   > Controls
      > How to allow revisions when many sets?
         > clear
         > remove set <drop down>
}

Documentation {
   > Pitfalls
      > angles far from perpendicular will result in innacuracies.
}

Tasks {
   > Protractor/pedastal
      > Font looks like it will have an inner and an outer.
         > Possibly recolour?
      > Confirm whether there will be issues aligning the design to the
        bed.

   > Read data sets
      > angle, depth[, tag]
   > Process datasets into objects
   > Plot objects
      > depths
      > resultant shape
      > indices/depth labels?
      > tag labels
   > transform later objects to match earlier objects tag locations.
   > Controls
      > Open files/Enter values directly?
      > Save
   > Error margins?
}
